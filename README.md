# 🏨 Restful Booker API Automation

Автоматизированные API тесты для сервиса бронирования [Restful Booker](https://restful-booker.herokuapp.com/).
Проект демонстрирует построение тестового фреймворка на Python с использованием Playwright и генерацией отчетов Allure.

## 🛠 Стек технологий
*   **Python 3.12**
*   **Playwright (APIRequestContext)** — для быстрых HTTP-запросов
*   **Pytest** — фикстуры и раннер тестов
*   **Allure Report** — подробные отчеты о прохождении
*   **GitHub Actions** — CI/CD для автозапуска тестов

## 📋 Реализованные сценарии
*   **Auth**: Получение токена авторизации (использование `scope='session'` для оптимизации).
*   **CRUD Booking**: Полный цикл работы с бронированием:
    *   `GET` — Получение списка и одной записи.
    *   `POST` — Создание новой брони (динамическая генерация данных).
    *   `PUT` — Полное обновление данных (с токеном).
    *   `PATCH` — Частичное обновление.
    *   `DELETE` — Удаление и проверка отсутствия записи (404).

## 🚀 Запуск локально

1. **Клонировать репозиторий:**
   ```bash
   git clone https://github.com/Dannyakenpachi/Restful_booker_api_test_playwright.git
   cd Restful_booker_api_test_playwright
2. **Установить зависимости:**
   ```bash
   pip install -r requirements.txt
   playwright install chromium
3. **Запустить тесты:**
   ```bash
   pytest --alluredir=allure-results
4. **Открыть отчет:**
   ```bash
   allure serve allure-results
## 📊 CI/CD Report
Тесты запускаются автоматически в GitHub Actions при каждом обновлении кода.
**[Посмотреть онлайн-отчет Allure](https://dannyakenpachi.github.io/Restful_booker_api_test_playwright/)**

   
